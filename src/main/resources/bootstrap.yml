spring:
  profiles:
    active: "test"
  cloud:
    gcp:
      secretmanager:
        enabled: true
      project-id: trading-326621
#      credentials:
#        location: file://serviceaccount.json

# logging
logging:
  level:
    root: INFO
    com.google: INFO
    uk.co.avsoftware: DEBUG

---
spring:
  config:
    activate:
      on:
        profile: production
  cloud:
    gcp:
      secretmanager:
        enabled: true

---

spring:
  config:
    activate:
      on:
        profile: test
  cloud:
    gcp:
      secretmanager:
        enabled: true