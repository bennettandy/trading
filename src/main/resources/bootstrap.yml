spring:
  profiles:
    active: "test"
  cloud:
    gcp:
      secretmanager:
        enabled: true
      project-id: trading-326621

# logging
logging:
  level:
    root: INFO
    com.google: INFO
    uk.co.avsoftware: DEBUG

---

spring:
  profiles: "production"

  cloud:
    gcp:
      secretmanager:
        enabled: true

---

spring:
  profiles: "test"
  cloud:
    gcp:
      secretmanager:
        enabled: true